import React, { Fragment } from "react";
import Header from "../components/header/Header";

const MyRoutes = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default MyRoutes;
